[project]
name = "module-05-testing-code-quality"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]

[tool.pytest.ini_options]
testpaths = ["03-exercises"]
addopts = ["-v", "--tb=short"]